
.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop
    

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop
    

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop
    

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop
    

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop
    

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop


.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop

.import >>from_external ext>ixt
.export >>from_external>d ext>ixt


main:
    mov r1 r3
    mov lr4
>loop:
    add r5 r6 #5
    !b >>cond
    !b:ne >loop>cond

>>cond: 
    cmp r4 r5
    !b >loop
    
    